include classpath("application.conf")

node-address {
    protocol = http
    host = ${HOSTNAME}
    port = 9324
    context-path = ""
}

rest-sqs {
    enabled = true
    bind-port = 9324
    bind-hostname = "0.0.0.0"
    sqs-limits = relaxed
}

include "queues.conf"
